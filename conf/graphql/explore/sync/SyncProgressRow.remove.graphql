# Generated File
# Remove a single SyncProgressRow entity from the system
mutation SyncProgressRowRemove {
  syncProgressRow {
    remove(key: "") {
      key
      status
      message
      lastTime
    }
  }
}
