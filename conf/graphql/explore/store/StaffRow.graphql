# Generated File
# Queries the system for Staff.
query StaffRow {
  staffRow(
    q: null, # Or string literal
    filters: null, # Or filters of type `{ k: "", o: Equal, v: "" }`
    orderBy: null, # Or orderBy of type `{ col: "", dir: Ascending }`
    limit: null, # Or number
    offset: null # Or number
  ) {
    totalCount
    paging {
      current
      next
      itemsPerPage
    }
    results {
      staffId
      firstName
      lastName
      addressId
      email
      storeId
      active
      username
      password
      lastUpdate
      picture
    }
    durationMs
    occurred
  }
}
