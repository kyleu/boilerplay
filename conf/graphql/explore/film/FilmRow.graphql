# Generated File
# Queries the system for Films.
query FilmRow {
  filmRow(
    q: null, # Or string literal
    filters: null, # Or filters of type `{ k: "", o: Equal, v: "" }`
    orderBy: null, # Or orderBy of type `{ col: "", dir: Ascending }`
    limit: null, # Or number
    offset: null # Or number
  ) {
    totalCount
    paging {
      current
      next
      itemsPerPage
    }
    results {
      filmId
      title
      description
      releaseYear
      languageId
      originalLanguageId
      rentalDuration
      rentalRate
      length
      replacementCost
      rating
      lastUpdate
      specialFeatures
      fulltext
    }
    durationMs
    occurred
  }
}
