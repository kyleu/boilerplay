{
  // Start enum paths
  // Projectile export section [boilerplay]
  "#include:settings/settingKeyType.json": "*",
  // End enum paths

  // Start schema paths
  // Projectile export section [boilerplay]
  "#include:sync/syncProgressRow.json": "*",
  "#include:task/scheduledTaskRunRow.json": "*",
  // End schema paths

  "#include:common.json": "*"
}
